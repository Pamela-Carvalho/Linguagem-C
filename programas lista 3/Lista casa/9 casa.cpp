#include<stdio.h>
#include<conio.h>

int main ()
{
	int a=1;
	do{
		if(a%2==0)
		{printf("\n%d par", a);}
		else
		printf("\n%d impar", a);
		a++;
		
		
		
		
		
		
	}
	while (a<=20);
	
	getch();
	return 0;
}
