#include<stdio.h>
#include<conio.h>

int main()
{
    int n, y;
    printf("entre com o numero: ");
    scanf("%d", &n);
    y=n*n;
    printf("o quadrado de n e: %d", y);
    
    getch();
    return 0;
}
